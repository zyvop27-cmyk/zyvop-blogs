# Postgres connection — stores draft job records (status, result, history)
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres
DATABASE_NAME=jobs_demo

# Redis connection — backs the BullMQ queue itself
REDIS_HOST=localhost
REDIS_PORT=6379

PORT=3000
